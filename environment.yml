# Conda 环境：AIma
# 创建：conda env create -f environment.yml
# 更新：conda env update -f environment.yml
# 激活：conda activate aima
name: aima
channels:
  - conda-forge
dependencies:
  - python>=3.11,<3.14
  - pip
  - pip:
      - pytest>=7
      - ruff>=0.3
