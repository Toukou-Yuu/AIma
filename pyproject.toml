[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "aima"
version = "0.0.1"
description = "Riichi mahjong engine (kernel)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-cov>=5", "ruff>=0.3"]
llm = ["httpx>=0.27", "python-dotenv>=1.0", "pyyaml>=6.0"]
rich = [
    "pillow>=10.0",
    "prompt-toolkit>=3.0",
    "questionary>=2.0",
    "rich>=14.2",
    "textual>=8.2",
    "wcwidth>=0.2",
]
experiment = ["pydantic>=2", "pyyaml>=6"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["kernel*", "llm*", "ui*", "arena*", "policies*", "agents*", "prompts*", "context*", "memory*", "models*", "experiments*", "metrics*", "replay*", "profiles*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "ui: tests requiring textual/questionary/rich UI libraries",
    "live: tests requiring real API calls (skipped by default)",
    "slow: tests that take >5 seconds to run",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.coverage.run]
source = ["src"]
omit = ["src/ui/*"]

[tool.coverage.report]
show_missing = true
fail_under = 74

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
