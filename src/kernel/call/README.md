# `kernel.call`

对他家舍牌的应答：荣和收集、碰/大明杠轮询、上家吃；与 `BoardState.turn_phase == CALL_RESPONSE` 及 `CallResolution` 配合。荣和形判定当前仅接入门清七对子示例，完整和了形见后续里程碑。

大明杠成立后的岭上摸与翻表宝指示牌由 `kernel.kan.apply_after_kan_rinshan_draw` 在 `apply_open_meld` 的 `DAIMINKAN` 分支末尾调用。
